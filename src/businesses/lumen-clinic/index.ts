// Barrel: every client folder exposes the same surface
// (`business` and `faqs`). The active-business selector imports from here.

export { business } from "./business";
export { faqs } from "./faq";
